//if there is no cookie page, open cookie page in a new tab
//if the cookie page already exists, just refresh the tab
function UpdateOrCreateTab(url) {
}

//listener for browser action
chrome.browserAction.onClicked.addListener(function(tab) {

});
